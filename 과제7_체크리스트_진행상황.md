# 과제7 진행상황 체크리스트 (Next.js + Supabase Auth 버전)

과제6 `index.html`(클라이언트 전용)의 Plan-Do-See 로직을 Next.js(App Router) + Supabase Auth/RLS 구조로 재구현했습니다.
이전에 시도했던 두 가지 다른 구현(Express+세션, Vercel 서버리스+JWT)은 `_archive/`로 옮겨 보관 중입니다 — 참고용일 뿐 제출 대상이 아닙니다.

---

## 1. 핵심 요구사항 — 상태

| 항목 | 상태 | 비고 |
|---|---|---|
| 첫 화면 = 로그인 화면 | ✅ 완료 | 비로그인 상태로 `/` 접근 시 즉시 `/login`으로 리다이렉트 — 로컬과 배포 주소(https://diary-nine-omega.vercel.app) 양쪽에서 실제 확인 |
| 결과물 주소(배포) | ✅ 완료 | `main` 브랜치 push → Vercel 자동 빌드 → https://diary-nine-omega.vercel.app 새 시크릿 창에서 로그인 화면 뜨는 것까지 확인 (2026-09-09) |
| 소스 주소(Git 이력 포함) | ✅ 완료 | 기존 저장소(`github.com/shk12170-dev/diary`)의 `main`에 그대로 이어서 커밋·푸시함(과제6 커밋이 조상으로 포함됨) |
| 인증 구현 설명서 6항목(ⓐ~ⓕ) | ✅ 작성 완료 | [과제7_인증구현설명서.md](과제7_인증구현설명서.md) — ⓔ/ⓕ 일부는 본인 경험으로 다듬기 권장 |
| 짧은 확인 방법 4줄 / AI와 내 판단 3줄 | ✅ 완료 | 위 문서 하단 |

## 2. 준비/수집 자료 — 상태

| 대상 | 상태 | 비고 |
|---|---|---|
| 비밀번호 암호화 | ✅ | Supabase Auth가 자체 처리(코드에서 비밀번호 문자열을 직접 다루지 않음) |
| 인증 방식 | ✅ | Supabase Auth(OAuth/인증 서비스 카테고리) — 이유는 설명서 ⓑ |
| 인가(타인 데이터 차단) | ✅ 완료, 실측까지 확인 | `supabase/schema.sql`의 RLS 정책 적용 후 계정 A/B로 실제 캡처 완료 |
| 테스트 계정 2개 | ✅ 완료 | `alice.pds.card4@gmail.com` / `bob.pds.card4@gmail.com` — 관리자 API로 이메일 인증 완료 상태로 생성, 로그인 확인함 |
| 네트워크/API 로그 캡처 | ✅ 완료 | 로그인 실패, 비로그인 조회 차단, 계정 A↔B 상호 차단까지 전부 실제 캡처 완료 (`과제7_인증구현설명서.md` ⓓ, `evidence/evidence.txt`) |

## 3. 카드별 체크리스트 — 상태

### 카드 1~2: 인증 방식 선택 & 비밀번호 단방향 암호화
- [x] 인증 서비스 선택 + 이유 (`과제7_인증구현설명서.md` ⓑ)
- [x] 비밀번호가 어디에도 평문으로 남지 않음 (Supabase Auth 자체 처리, 코드에서 미접촉)
- [x] Salt 적용 실증 — Supabase Auth 표준 동작(직접 구현하지 않았으므로 자체 DB 조회로 재확인은 불필요, 서비스 문서 기준으로 설명서에 기술)

### 카드 3: 인증 유지 및 로그아웃 처리
- [x] Supabase 세션(JWT, 자동 갱신) 사용
- [x] 로그인 성공 vs 로그아웃/미인증 거절 응답 실제 캡처 (`과제7_인증구현설명서.md` ⓓ)
- [x] 토큰이 URL에 실리지 않음(Authorization 헤더로만 전달), `.env.local`은 `.gitignore`로 커밋 제외 확인 필요

### 카드 4: 인가 및 타인 데이터 접근 차단
- [x] RLS 정책 작성 완료 (`supabase/schema.sql`)
- [x] `supabase/schema.sql`을 Supabase 대시보드 SQL Editor에서 실행 (2026-09-09, 5개 테이블 + RLS 정책 생성 확인)
- [x] 테스트 계정 A/B 생성 + 로그인 확인
- [x] `node scripts/capture-evidence.mjs` 실행 → `evidence/evidence.txt` 생성 완료
- [x] A→B 읽기/수정/삭제 시도 전부 거절(빈 결과) 확인, 반대 방향(비로그인)도 거절 확인, A 자료 무결성 확인
- [x] `evidence/evidence.txt` 내용을 `과제7_인증구현설명서.md` ⓓ에 반영 완료

### 카드 5: 5일 실사용 데이터 & 설명서 완성
- [x] 설명서 6항목 틀 작성 (ⓔ/ⓕ 포함, 2026-09-09 기준 실제 내용으로 채움)
- [x] ⓔ, ⓕ, "AI 제안을 따르지 않은 일" — 본인 경험으로 다듬음
- [ ] **5일 실사용 기록** — 아래 실제 일정표대로 진행 (코드로 대신할 수 없음, 소급 작성 금지) — **2/5일차 완료**
- [x] 2~3일차 사이 집계 규칙 1개 변경 + 이유 기록 (아래 "집계 규칙 변경" 참고)
- [ ] 화면 합계/평균과 수기 검산 대조
- [ ] "JSON 파일 내보내기" 버튼으로 파일 1개 준비해 제출물에 첨부

**실제 진행 일정 (소급 아님 — 각 날짜에 실제로 접속해서 기록)**

| 일차 | 날짜 | 상태 | 해야 할 일 |
|---|---|---|---|
| 1일차 | 2026-09-04 | ✅ 완료 | `pds-diary-2026-09-04.json`을 배포 주소의 "JSON 파일 가져오기"로 실제로 불러옴 (할 일 6건, 실행기록 6건) |
| 2일차 | 2026-09-09 | ✅ 완료 | `admin1@admin.com` 계정으로 실제 접속 → "과제 7 플랜두씨 다이어리 5일 기록 2일차 진행" 등록·완료 처리 (실제 8h, 막힌 이유 "로그인/회원가입 창 구축간 오류로 지연"까지 기록, 완료 시각 09-09 10:56) |
| — | 2일차~3일차 사이 | ✅ 완료 | 집계 규칙 변경 (아래 참고) |
| 3일차 | 2026-09-10 | ⬜ 대기 | 바뀐 규칙으로 실제 기록 |
| 4일차 | 2026-09-11 | ⬜ 대기 | 바뀐 규칙으로 실제 기록 |
| 5일차 | 2026-09-12 | ⬜ 대기 | 바뀐 규칙으로 실제 기록 + 화면 합계와 수기 검산 대조 + JSON 내보내기 |

**집계 규칙 변경 (2026-09-10, 2일차→3일차 사이)**

- **기존 규칙**: "지연"은 `상태 = TODO(미완료) && 마감일 < 오늘`인 경우만 집계. 마감일을 넘겨서 완료 처리해도 상태가 `DONE`이 되는 순간 지연 통계에서 완전히 빠짐.
- **바뀐 규칙**: 완료(`DONE`)했더라도 실제 완료 시점(실행기록의 종료 시각)이 마감일보다 늦으면 "지연완료"로 지연 수에 포함. 화면 목록에서도 해당 항목은 `✅ 완료` 대신 `⚠️ 지연완료`로 별도 표시.
- **바꾼 이유**: 2일차를 실제로 써보니, 마감일을 넘겨서 완료 처리해도 화면엔 그냥 "완료"로만 뜨고 "돌아보기" 통계의 지연 수에는 전혀 반영되지 않았습니다. 이러면 실제로는 일정이 밀렸는데도 성과 지표상으로는 아무 문제 없어 보이는 착시가 생깁니다. 그래서 완료 시점 자체가 마감일을 넘긴 경우도 지연으로 잡히도록 규칙을 바꿨습니다.
- **구현/검증**: [components/Diary.jsx](components/Diary.jsx)의 `isLateCompletion()` 함수로 구현. 테스트 계정으로 마감일이 지난 뒤 완료 처리한 항목을 만들어 화면에 `⚠️ 지연완료`로 표시되고 "지연 수" 통계에 정확히 반영되는 것까지 실제 확인함.

> 참고: 이전에 작성된 `5일_실사용_기록_및_규칙변경.md`는 지금과 다른 구현(JWT 버전) 작업 로그를 기반으로 한 예시 데이터라서 `_archive/`로 옮겼습니다. 지금 구조(Supabase 버전)로 위 일정대로 실제 사용한 뒤 새로 작성해야 합니다. 제출 기한상 위 일정을 다 채우기 어렵다면, 그건 담당 교수/조교와 상의할 문제이지 기록을 소급해서 채워 넣을 문제가 아닙니다.

---

## 4. 남은 일 (사람이 직접 해야 하는 것)

코드·DB·인가 검증은 모두 끝났습니다. 아래는 실제 시간이 걸리거나 본인 계정으로만 가능한 것들입니다.

1. ~~본인 이메일로 실제 회원가입~~ → 완료: `admin1@admin.com`으로 정착, 위 카드5 일정표대로 이 계정으로 진행.
2. **실제 5일간 사용** — 위 카드5 일정표(9/4 완료, 9/9~9/12 진행 예정)대로 진행. 소급 작성 불가.
3. ~~설명서 ⓔ, ⓕ~~ → 완료.
4. **테스트 계정 정리(선택)** — `alice.pds.card4@gmail.com` / `bob.pds.card4@gmail.com`은 카드4 검증 전용으로 만든 계정입니다. 제출 후 Supabase 대시보드 → Authentication → Users에서 삭제해도 됩니다. (`admin@admin.com`, `admin1@admin.com`은 실제 5일 기록용이니 삭제하지 마세요.)
5. **자동화에 쓴 키 무효화(권장, 배포에는 영향 없음)** — 이번 세션에서 받은 Supabase secret 키와 Vercel 토큰은 각각의 대시보드에서 재발급/삭제하면 됩니다.
   - **Vercel 토큰**: 배포 자체는 Vercel의 GitHub 연동으로 돌아가고, 이 토큰은 제가 API로 배포를 조작할 때만 쓴 것이라 무효화해도 이미 배포된 사이트나 앞으로의 `git push` 자동배포에는 영향이 없습니다.
   - **Supabase secret(service_role) 키**: 배포된 앱은 이 키를 전혀 쓰지 않습니다(`.env.local`의 공개용 `NEXT_PUBLIC_SUPABASE_ANON_KEY`만 사용, 서로 다른 키). secret 키는 제가 로컬에서 관리자 작업(테스트 계정 생성/인증)에만 썼으므로 무효화해도 로그인·다이어리 기능은 그대로 작동합니다.
   - ⚠️ 단, `NEXT_PUBLIC_SUPABASE_ANON_KEY`(공개 anon 키)는 절대 재발급하지 마세요 — 이건 배포된 앱이 실제로 쓰는 키라 바뀌면 사이트가 깨집니다.

## 5. 파일 위치

```
.
├── app/
│   ├── page.js            # "/" 메인 다이어리 (세션 없으면 /login으로 리다이렉트)
│   ├── login/page.js       # "/login" 로그인·회원가입
│   ├── layout.js, globals.css
├── components/
│   ├── AuthForms.jsx        # 로그인/회원가입 UI
│   └── Diary.jsx             # Plan-Do-See 전체 기능 (Supabase CRUD)
├── lib/supabase.js           # Supabase 클라이언트
├── supabase/schema.sql        # 테이블 + RLS 정책 (대시보드에서 1회 실행 필요)
├── scripts/capture-evidence.mjs  # 카드4 증거 자동 캡처 스크립트
├── evidence/evidence.txt      # 위 스크립트 실행 결과 (실행 전까지는 없음)
├── 과제7_인증구현설명서.md      # 제출용 핵심 문서
├── 과제7_체크리스트_진행상황.md  # 이 문서
├── contracts/pds-schema-v2.json # 과제6 스키마 계약(참고용)
└── _archive/                   # 채택하지 않은 구현(plando-diary, api) 보관
```
