# 인증 구현 설명서 — 플랜두씨 다이어리 2 (과제 7)

> **보안 준수 사항**
> 1. 이 문서의 토큰/키 값은 앞부분만 남기고 마스킹했습니다 (`sb_publishab...[마스킹]`).
> 2. 실제 사용자의 비밀번호 평문은 문서·코드·DB 어디에도 기록되지 않습니다(Supabase Auth가 내부적으로 해시 저장).
> 3. ⓓ의 요청/응답 기록은 로컬 개발 서버(`npm run dev`)에서 실제 Supabase 프로젝트에 보낸 요청을 그대로 캡처한 것입니다.

---

## ⓐ 무엇으로 붙였나

- **인증 서비스**: [Supabase Auth](https://supabase.com/auth) (이메일/비밀번호 방식, 내부적으로 GoTrue + JWT 사용)
- **비밀번호 저장**: Supabase Auth가 자체적으로 단방향 해시(bcrypt 계열)로 저장 — 이 프로젝트 코드는 비밀번호 문자열을 한 번도 직접 다루지 않음
- **인가(Authorization)**: Postgres **Row Level Security(RLS)** — `plans`, `todos`, `execution_records` 등 모든 테이블에 `auth.uid() = user_id` 정책을 걸어, DB 자체가 "내 행만" 반환/수정하도록 강제
- **클라이언트 상태 유지**: `@supabase/supabase-js`가 세션(JWT access token + refresh token)을 브라우저에 보관하고, 만료 시 자동 갱신. 모든 DB 요청에 이 세션 토큰이 자동으로 실려 감
- **화면 진입 제어**: `app/page.js`(메인 다이어리, `/`)와 `app/login/page.js`(로그인/가입, `/login`)가 각각 세션 유무를 확인해 서로 반대 방향으로 리다이렉트 — 비로그인 상태로 `/`에 들어오면 즉시 `/login`으로 이동

## ⓑ 왜 그걸 골랐나

- **직접 JWT/세션을 구현하지 않고 Supabase Auth를 고른 이유**: 배포 환경이 이미 Vercel + Supabase(`.env.local`에 `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` 존재)로 정해져 있었습니다. 직접 만든 로그인 API를 서버리스 함수로 얹으면 비밀키 관리·토큰 재발급·비밀번호 재설정 같은 걸 전부 손으로 구현해야 하는데, 이미 검증된 인증 서비스가 배포 스택에 붙어 있는 상황에서 이를 다시 만드는 건 불필요한 위험이라 판단했습니다.
- **RLS로 인가를 처리한 이유**: 앱 코드에서 매 쿼리마다 `WHERE user_id = ...`를 손으로 넣는 방식은, 개발자가 실수로 한 곳이라도 빼먹으면 그 즉시 타인 데이터가 새어 나갑니다. RLS는 **DB 레벨에서 강제**되므로, 프론트엔드 코드에 버그가 있어도(예: 필터를 깜빡해도) 서버가 애초에 타인의 행을 돌려주지 않습니다. "인가 로직을 앱이 아니라 DB가 책임진다"는 게 가장 견고한 방어선이라고 판단해 선택했습니다.

## ⓒ 어디를 어떻게 고쳤나

과제6은 `index.html` 하나에 전부 들어있는 `localStorage` 전용 앱이었습니다. 과제7에서는 Next.js(App Router) + Supabase로 아래와 같이 재구성했습니다.

| 파일 | 역할 |
| --- | --- |
| [lib/supabase.js](lib/supabase.js) | `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`로 Supabase 클라이언트 생성 |
| [app/page.js](app/page.js) | `/` — 세션 확인 후 없으면 `/login`으로 리다이렉트, 있으면 `<Diary>` 렌더 |
| [app/login/page.js](app/login/page.js) | `/login` — 세션이 있으면 `/`로 리다이렉트, 없으면 `<AuthForms>` 렌더 |
| [components/AuthForms.jsx](components/AuthForms.jsx) | 로그인/회원가입 탭 UI. `supabase.auth.signInWithPassword` / `supabase.auth.signUp` 호출 |
| [components/Diary.jsx](components/Diary.jsx) | 과제6의 Plan-Do-See 기능(계획/할 일/완료 기록/돌아보기/내보내기) 전체를 Supabase 테이블 CRUD로 재구현. 로그아웃 버튼(`supabase.auth.signOut`) 포함 |
| [supabase/schema.sql](supabase/schema.sql) | `plans`, `plan_histories`, `todos`, `execution_records`, `review_action_items` 테이블 + 테이블마다 `auth.uid() = user_id` RLS 정책. Supabase 대시보드 SQL Editor에서 1회 실행 필요 |
| [scripts/capture-evidence.mjs](scripts/capture-evidence.mjs) | 계정 A/B로 실제 로그인해 카드4(타인 데이터 접근 차단) 시나리오를 자동 재현하고 `evidence/evidence.txt`에 기록하는 스크립트 |

### 가입 · 로그인 · 인가 처리 흐름

| 흐름 | 프론트엔드 | 백엔드(Supabase) |
| --- | --- | --- |
| 회원가입 | `AuthForms`에서 `supabase.auth.signUp({email, password})` 호출 | Auth가 계정 생성, 비밀번호 해시 저장, (프로젝트 설정에 따라) 인증 메일 발송 |
| 로그인 | `supabase.auth.signInWithPassword({email, password})` | 자격 증명 검증 후 JWT 세션 발급 |
| 화면 진입 제어 | `page.js` / `login/page.js`가 `getSession()` + `onAuthStateChange`로 감시, 세션 유무에 따라 `router.replace()` | - |
| 내 자료 조회 | `supabase.from('todos').select('*')` 등 (토큰은 클라이언트가 자동으로 첨부) | RLS가 `auth.uid() = user_id`인 행만 반환 |
| 타인 자료 차단 | 별도 분기 없음 — 애초에 응답에 안 들어있음 | 타인 소유 행은 조회/수정/삭제 요청에서 **빈 결과**로 응답 (행이 "존재하지 않는 것"처럼 보임) |
| 로그아웃 | `supabase.auth.signOut()` | 로컬 세션 폐기, 이후 요청은 무권한(anon) 취급 |

## ⓓ 안 열리는 것을 확인한 기록 (성공 vs 거절 요청 대조)

### 1) 잘못된 비밀번호로 로그인 시도 → 거절 (실제 캡처)
```http
POST /auth/v1/token?grant_type=password HTTP/1.1
Host: khhacjimdbfrwtldsesc.supabase.co
apikey: sb_publishab...[마스킹]
Content-Type: application/json

{"email":"alice.pds01@gmail.com","password":"WrongPassword999"}

HTTP/1.1 400 Bad Request
x-sb-error-code: invalid_credentials

{"code":400,"error_code":"invalid_credentials","msg":"Invalid login credentials"}
```

### 2) 이메일 인증 전 계정으로 로그인 시도 → 거절 (실제 캡처, 브라우저에서 재현)
회원가입 직후(이메일 인증 전) 같은 자격 증명으로 로그인하면:
```
로그인 실패: Email not confirmed
```
→ 가입만으로는 로그인이 되지 않고, 반드시 이메일 인증 링크를 눌러야 세션이 발급됨을 확인했습니다.

### 3) 로그인 없이(anon key만으로) 보호 자료 조회 시도 → 빈 결과 (실제 캡처)
```http
GET /rest/v1/todos?select=* HTTP/1.1
Authorization: Bearer sb_publishab...[마스킹]  (anon key, 로그인 세션 아님)

HTTP/1.1 200 OK
[]
```
→ 401로 아예 막는 대신, RLS가 "내가 소유한 행이 없다"는 결과(빈 배열)를 돌려줍니다. 타인이 자료가 존재하는지조차 알 수 없다는 점에서, JWT 방식에서 흔히 쓰는 "404로 뭉뚱그려 거절"과 같은 정보 은닉 효과를 DB 레벨에서 얻습니다.

### 4) 계정 A ↔ 계정 B 상호 접근 차단 (실제 캡처, `scripts/capture-evidence.mjs` 실행 결과 — `evidence/evidence.txt`)

**A가 자기 할 일 생성 → 성공**
```http
POST /rest/v1/todos HTTP/1.1
Authorization: Bearer eyJhbGciOiJF...[A 토큰 마스킹]

HTTP/1.1 201 Created
[{"id":"b5f97ab4-59a8-4ae4-ad70-3e446b3e2a23","user_id":"023a7972-...","title":"과제7 카드4 검증용 할 일", ...}]
```

**B가 A의 할 일을 id로 직접 조회 시도 → 거절 (빈 배열)**
```http
GET /rest/v1/todos?id=eq.b5f97ab4-59a8-4ae4-ad70-3e446b3e2a23 HTTP/1.1
Authorization: Bearer eyJhbGciOiJF...[B 토큰 마스킹]

HTTP/1.1 200 OK
[]
```
→ 401/403/404 대신 **빈 배열**로 응답합니다. RLS는 "권한 없음"이 아니라 "그런 행이 애초에 없다"는 결과를 주기 때문에, 타인이 그 ID의 자료가 실제로 존재하는지조차 알 수 없습니다.

**B가 A의 할 일을 수정(PATCH) 시도 → 거절 (변경된 행 0건)**
```http
PATCH /rest/v1/todos?id=eq.b5f97ab4-59a8-4ae4-ad70-3e446b3e2a23 HTTP/1.1
Authorization: Bearer eyJhbGciOiJF...[B 토큰 마스킹]

HTTP/1.1 200 OK
[]
```

**B가 A의 할 일을 삭제(DELETE) 시도 → 거절 (삭제된 행 0건)**
```http
DELETE /rest/v1/todos?id=eq.b5f97ab4-59a8-4ae4-ad70-3e446b3e2a23 HTTP/1.1
Authorization: Bearer eyJhbGciOiJF...[B 토큰 마스킹]

HTTP/1.1 200 OK
[]
```

**A가 재조회 → 원본 그대로(무결성 확인)**
```http
GET /rest/v1/todos?id=eq.b5f97ab4-59a8-4ae4-ad70-3e446b3e2a23 HTTP/1.1
Authorization: Bearer eyJhbGciOiJF...[A 토큰 마스킹]

HTTP/1.1 200 OK
[{"id":"b5f97ab4-59a8-4ae4-ad70-3e446b3e2a23","title":"과제7 카드4 검증용 할 일", ...}]
```
→ B의 수정·삭제 시도 이후에도 A의 자료는 title, 값 모두 변조되지 않고 그대로였습니다.

**B의 목록 조회에도 A의 자료가 섞이지 않음**
```http
GET /rest/v1/todos HTTP/1.1
Authorization: Bearer eyJhbGciOiJF...[B 토큰 마스킹]

HTTP/1.1 200 OK
[]
```

**로그아웃(토큰 없음) 상태로 같은 자료 재조회 → 거절**
```http
GET /rest/v1/todos?id=eq.b5f97ab4-59a8-4ae4-ad70-3e446b3e2a23 HTTP/1.1
(Authorization 헤더 없음, anon key만)

HTTP/1.1 200 OK
[]
```

전체 원본 로그: [evidence/evidence.txt](evidence/evidence.txt)

### 5) 로그아웃 후 재사용 거절
로그아웃(`signOut()`)은 로컬 세션을 즉시 폐기합니다. 이후 같은 화면에서 다이어리 데이터를 요청하는 코드 경로 자체가 실행되지 않으며(로그인 화면으로 전환), 남은 access token으로 직접 API를 호출해도 만료 후에는 401로 거절됩니다.

## ⓔ AI와 나 (AI 활용 내역)

- **AI에게 맡긴 일**: Next.js App Router 구조에서 세션 기반 리다이렉트 게이트(`page.js` ↔ `login/page.js`) 뼈대 작성, `supabase.auth` 호출부·RLS 정책 SQL 초안, 과제6 `index.html`의 Plan-Do-See 로직을 React 컴포넌트로 옮기는 반복 작업, curl/Node 스크립트로 실제 Supabase 프로젝트에 요청을 보내 응답을 캡처.
- **내가 직접 판단한 일**: 세 가지 후보 구현(과거 세션 기반 Express 서버 / Vercel 서버리스+JWT / Next.js+Supabase Auth) 중 이미 배포된 인프라(Vercel, Supabase 키)에 맞춰 Supabase Auth+RLS를 최종 채택. 인가 실패 시 앱 코드에서 조건 분기를 두는 대신 RLS로 "존재 자체를 감추는" 방식을 선택.
- **AI 제안을 따르지 않은 일**: (직접 채워주세요 — 예: 이메일 인증을 끌지, 켠 채로 둘지 등 실제로 본인이 고민하고 결정한 부분을 적으면 좋습니다.)

## ⓕ 아직 못 막은 것 (솔직한 보안 한계점)

1. **무차별 대입 공격(Brute-Force) 방어는 Supabase 기본값에 의존**: 자체적으로 rate limit이나 계정 잠금을 추가로 구현하지 않았습니다. Supabase 프로젝트의 기본 rate limit에 의존하는 상태입니다.
2. **비밀번호 복잡도 정책 미적용**: 6자 이상이라는 최소 길이만 프론트엔드에서 검사하며, 특수문자/숫자 조합 등은 강제하지 않습니다.
3. **anon key는 브라우저에 그대로 노출됨**: Supabase 설계상 의도된 것이지만(anon key는 공개 키), 접근 제어는 전적으로 RLS 정책에 달려 있습니다. `supabase/schema.sql`의 정책 중 하나라도 잘못 적용되면 그 테이블은 즉시 노출될 수 있습니다.
4. **비밀번호 재설정 플로우 미구현**: 비밀번호를 잊었을 때 복구할 UI가 없습니다(Supabase Auth 자체는 지원하나 이 앱에서 화면을 만들지 않음).
5. **토큰 블랙리스트 없음**: 로그아웃은 로컬 세션만 지우며, 탈취된 refresh token은 만료 전까지 이론상 재사용될 수 있습니다.

---

## 짧은 확인 방법 (4줄)

1. **어디로 가나요**: 배포된 주소(`/`)로 접속합니다.
2. **3단계 안에 무엇을 하나요**: ① 회원가입 후 이메일 인증 → 로그인 ② 계획/할 일 1건 등록 ③ 로그아웃 후 새로고침.
4. **무엇이 보이면 통과인가요**: 로그인 전에는 `/login` 화면만 보이고, 로그인해야만 `/`에서 다이어리가 보이며, 로그아웃 즉시 `/login`으로 돌아가면 통과입니다.
4. **안 될 때 무엇이 보이나요**: 로그인하지 않았는데 다이어리가 보이거나, 로그아웃 후에도 데이터가 계속 조회되면 실패입니다.

## AI와 내 판단 (3줄)

- **AI에게 맡긴 일**: Next.js 리다이렉트 게이트, Supabase 클라이언트 연동, RLS 정책 SQL, 과제6 로직의 React 이식, 실제 API 호출 증거 캡처.
- **내가 직접 판단한 일**: 세 가지 구현 후보 중 배포 인프라에 맞는 Supabase Auth+RLS 최종 선택, 타인 데이터 차단 방식(빈 결과로 정보 은닉)을 앱 로직이 아닌 DB 정책으로 강제하기로 결정.
- **AI 제안을 따르지 않은 일**: (본인 경험으로 채워주세요.)
