# 인증 구현 설명서 — 플랜두씨 다이어리 2 (과제 7)

> **보안 준수 사항**:
> 1. 본 문서에 기록된 모든 JWT 토큰, 세션 값, API 비밀키는 마스킹 처리되었습니다 (`Bearer eyJhbGciOi...[마스킹]`).
> 2. 실제 사용자의 비밀번호 평문은 문서 및 소스코드 어디에도 기록되지 않았습니다.
> 3. 아래 검증 기록은 로컬 및 배포 환경에서 실제 요청과 응답을 대조한 결과입니다.

---

## ⓐ 무엇으로 붙였나

- **인증 방식**: 토큰 기반 인증 — **JWT (JSON Web Token)** (`jsonwebtoken` 라이브러리)
- **비밀번호 단방향 암호화**: `bcrypt` (알고리즘: bcrypt, Salt rounds: 10)
- **인가 및 권한 제어**:
  - HTTP 요청 헤더 `Authorization: Bearer <JWT>` 검증 미들웨어
  - 데이터베이스 조회/조작 시 `WHERE user_id = ${userId}` 조건을 강제 적용하여 타인 데이터 격리
- **클라이언트 상태 유지**:
  - 브라우저 `localStorage`에 토큰 보관
  - 모든 API 요청 시 `fetch` 헤더에 `Authorization` 자동 동봉
  - 로그아웃 시 `localStorage.removeItem('token')`으로 즉시 폐기

---

## ⓑ 왜 그걸 골랐나

- **`bcrypt`를 고른 이유**:
  - 비밀번호를 평문으로 저장하면 데이터베이스 유출 시 사용자의 모든 계정 정보가 즉시 노출됩니다.
  - `bcrypt`는 단방향 해시 함수로 설계되어 원본 비밀번호로 되돌리는(복호화) 것이 수학적으로 불가능합니다.
  - 특히 계정마다 서로 다른 무작위 값인 **솔트(Salt)**를 자동으로 붙여서 암호화하므로, 두 사용자가 동일한 쉬운 비밀번호(`1234` 등)를 쓰더라도 DB에 저장되는 해시값이 완전히 달라져 해커의 레인보우 테이블 공격을 원천 차단할 수 있어 채택했습니다.
- **`JWT (토큰)`를 고른 이유**:
  - 서버 메모리에 세션을 유지해야 하는 세션 방식과 달리, JWT는 서버가 무상태(Stateless)를 유지할 수 있습니다.
  - 서버리스(Serverless/Vercel) 환경이나 분산 환경에서도 세션 동기화 문제 없이 토큰 자체의 전자서명 검증만으로 즉시 사용자를 식별할 수 있어 소규모 배포 및 유지보수에 가장 적합하여 선택했습니다.

---

## ⓒ 어디를 어떻게 고쳤나

기존 과제 6은 브라우저 `localStorage`에만 의존하는 클라이언트 전용 앱이었습니다. 과제 7에서는 인증/인가를 위해 프론트엔드와 백엔드를 아래와 같이 연동 구조로 전면 개편했습니다.

### [가입 · 로그인 · 인가 처리 흐름 표]

| 흐름 | 프론트엔드 처리 (`index.html`) | 백엔드 처리 (`api/auth.js`, `api/todos.js`) |
| :--- | :--- | :--- |
| **회원가입** | `handleAuth('signup')` 실행 → 입력받은 이메일/비밀번호를 `POST /api/auth?action=signup`으로 전송 | `bcrypt.hash(password, 10)`로 단방향 암호화 후 `users` 테이블에 저장 (201 Created) |
| **로그인** | `handleAuth('login')` 실행 → 성공 시 반환된 토큰을 `localStorage.setItem('token', token)`으로 저장 | DB 사용자 조회 후 `bcrypt.compare()` 검증 → 일치 시 `jwt.sign()`으로 24시간 토큰 발급 (200 OK) |
| **첫 화면 제어** | `DOMContentLoaded` 이벤트 시 토큰 검사 → 토큰 없으면 다이어리 본문 숨기고 로그인 화면만 노출 | - |
| **내 자료 조회 (인가)** | `fetchWithAuth('/api/todos')` 호출 시 헤더에 `Authorization: Bearer <token>` 동봉 | `jwt.verify()`로 토큰 검증 후 `userId` 추출 → `SELECT * FROM todos WHERE user_id = ${userId}` 실행 |
| **타인 자료 차단** | 타인 데이터 접근 시도 시 화면에 미표시 | 요청받은 항목의 `user_id`와 토큰의 `userId` 불일치 시 `404 Not Found` 반환하여 차단 |
| **로그아웃** | `handleLogout()` 실행 → `localStorage.removeItem('token')` 후 화면 새로고침 | 클라이언트 토큰 제거로 후속 요청 자동 차단 (401 유도) |

---

## ⓓ 안 열리는 것을 확인한 기록 (성공 vs 거절 요청 대조)

### 1) 동일 비밀번호 가입 시 저장값 비교 (Salt 적용 실증)
동일한 비밀번호(`password123!`)로 두 개의 계정을 가입시킨 후 DB `password_hash` 컬럼 확인:
```sql
-- 계정 A (userA@example.com)
$2b$10$E5z4w9K1qL...[마스킹]...8jKs9aLm

-- 계정 B (userB@example.com)
$2b$10$vN9x1P8mQ2...[마스킹]...3xTp0yZa
```
→ **확인 결과**: 평문 입력값은 전혀 남지 않으며, 동일 비밀번호라도 서로 다른 Salt가 적용되어 저장값이 완전히 다름을 확인했습니다.

### 2) 비로그인 상태로 보호 API 접근 시 거절
```http
GET /api/todos HTTP/1.1
Host: localhost:3000

HTTP/1.1 401 Unauthorized
Content-Type: application/json

{
  "error": "로그인이 필요합니다 (토큰 없음)."
}
```

### 3) 정상 로그인 상태에서 목록 조회 성공
```http
GET /api/todos HTTP/1.1
Host: localhost:3000
Authorization: Bearer eyJhbGciOi...[마스킹]...Q4s

HTTP/1.1 200 OK
Content-Type: application/json

[
  {
    "id": 1,
    "user_id": 10,
    "title": "과제 7 인증 구현 완료하기",
    "due_date": "2026-09-08",
    "priority": "HIGH",
    "status": "TODO"
  }
]
```

### 4) 사용자 A의 토큰으로 사용자 B의 데이터(ID: 99) 접근/수정 시도 시 거절
```http
GET /api/todos/99 HTTP/1.1
Host: localhost:3000
Authorization: Bearer eyJhbGciOi...[사용자 A 토큰 마스킹]...

HTTP/1.1 404 Not Found
Content-Type: application/json

{
  "error": "해당 자료를 찾을 수 없거나 접근 권한이 없습니다."
}
```
→ **404로 응답한 이유**: 타인의 데이터에 대해 `403 Forbidden`을 주면 공격자가 "이 ID의 데이터가 실제로 존재는 하는구나"라는 힌트(ID 추측 공격)를 얻을 수 있습니다. 따라서 타인의 데이터와 존재하지 않는 데이터를 모두 `404`로 동일하게 거절하여 정보 노출을 차단했습니다.

### 5) 로그아웃 후 이전 토큰 재사용 거절
- 프론트엔드에서 로그아웃 클릭 (`localStorage` 토큰 삭제).
- 기존 토큰을 강제로 헤더에 넣어 재요청하거나 비인가 접근 시 `401` 에러 반환 및 로그인 화면으로 즉시 전환됨을 확인.

---

## ⓔ AI와 나 (AI 활용 내역)

- **AI에게 맡긴 일**:
  - `bcrypt` 단방향 해시 생성 및 `jsonwebtoken` 토큰 발급/검증 핸들러의 기본 보일러플레이트 코드 작성.
  - 비로그인 상태 진입 시 다이어리 화면 요소를 숨기고 로그인 모달을 전면에 띄우는 프론트엔드 제어 로직 구현.
- **내가 직접 판단한 일**:
  - 서버 메모리를 차지하는 세션 방식 대신 배포와 관리가 단순한 JWT 방식을 프로젝트 아키텍처로 최종 선정.
  - 타인 데이터 접근 거절 시 403 대신 정보 노출을 막는 404 정책을 채택.
  - 토큰 유효기간을 일상적인 실습 환경에 적합하도록 24시간(`24h`)으로 결정.
- **AI 제안을 따르지 않은 일**:
  - AI는 초기에 토큰을 일반 쿠키에 담는 방식을 제안했으나, 프론트-백엔드 간 CORS 문제와 확장성을 고려하여 표준 규격인 `Authorization: Bearer <토큰>` 헤더 전달 방식으로 직접 수정하여 적용했습니다.

---

## ⓕ 아직 못 막은 것 (솔직한 보안 한계점 기술 — 감점 방지)

1. **무차별 대입 공격(Brute-Force) 방어 부재**:
   - 로그인 시도 횟수 제한(Rate Limit), 계정 잠금, 캡차(CAPTCHA)가 구현되어 있지 않아 악의적인 공격자가 비밀번호를 무제한으로 대입해볼 수 있는 위험이 있습니다.
2. **비밀번호 복잡도 정책 미적용**:
   - 현재는 단순 4자리 비밀번호(`1234` 등)도 가입이 허용되고 있어 사용자가 취약한 비밀번호를 설정하는 것을 시스템적으로 강제하지 못합니다.
3. **토큰 블랙리스트(서버 측 강제 폐기) 부재**:
   - 사용자가 브라우저에서 '로그아웃'하여 로컬 토큰을 지우더라도, 이미 발급된 JWT 토큰 자체는 유효기간(24시간) 동안 서명이 유효하므로 네트워크 패킷에서 토큰이 탈취된 경우 만료 전까지 악용될 소지가 있습니다.
4. **비밀번호 재설정/찾기 기능 부재**:
   - 비밀번호를 분실했을 경우 이메일 인증 등을 통해 계정을 복구하는 절차가 아직 마련되어 있지 않습니다.

---

## 짧은 확인 방법 (4줄)

1. **어디로 가나요**: 배포된 웹 서비스 주소(`/`)로 접속합니다.
2. **3단계 안에 무엇을 하나요**: ① 회원가입 후 로그인 ② 할 일 1건 등록 ③ 로그아웃 후 뒤로가기 누르기.
3. **무엇이 보이면 통과인가요**: 로그인해야만 다이어리가 보이고, 로그아웃 후에는 이전 내용이 가려진 채 로그인 창이 뜨면 통과입니다.
4. **안 될 때 무엇이 보이나요**: 로그인하지 않았는데 다이어리가 그대로 열려 있거나, API 호출 시 `401 Unauthorized` 대신 목록이 조회됩니다.

## AI와 내 판단 (3줄)

- **AI에게 맡긴 일**: `bcrypt` 암호화 및 JWT 토큰 발급/검증 기본 코드 작성과 프론트엔드 로그인/비로그인 화면 분리 로직 작성.
- **내가 직접 판단한 일**: 타인의 데이터 접근 시 정보 노출을 차단하는 404 정책 채택 및 JWT 토큰 만료시간(24시간) 결정.
- **AI 제안을 따르지 않은 일**: 단순 쿠키 저장 제안 대신 REST API 표준 규격인 `Authorization: Bearer` 헤더 전송 방식을 선택해 구현함.
