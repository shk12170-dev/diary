# 과제7 진행상황 체크리스트

과제6 `index.html`(클라이언트 전용 버전)을 기반으로, 서버 인증/인가를 붙인 `plando-diary/` 프로젝트를 만들었습니다.
아래는 보내주신 요약(핵심요구사항 / 준비자료 / 단계별체크리스트) 기준으로 **무엇을 코드로 끝냈고, 무엇이 남았는지** 정리한 것입니다.

---

## 1. 핵심 요구사항 — 상태

| 항목 | 상태 | 비고 |
|---|---|---|
| 첫 화면 = 로그인 화면 | ✅ 완료 | `public/login.html`, `GET /` |
| 소스 주소(Git 이력 포함) | ⚠️ **직접 처리 필요** | 아래 "남은 일" 참고 |
| 인증 구현 설명서 6항목(ⓐ~ⓕ) | ✅ 초안 완료 | `AUTH_설명서.md` — ⓔ만 본인 언어로 다듬기 |
| 짧은 확인 방법 4줄 | ✅ 완료 | `AUTH_설명서.md` 하단 |
| AI와 내 판단 3줄 | ✅ 초안 완료 | 본인 경험으로 다듬기 |

## 2. 준비/수집 자료 — 상태

| 대상 | 상태 | 비고 |
|---|---|---|
| 과제6 소스코드 | ✅ 반영함 | 업로드해주신 `index.html` 스키마·로직을 그대로 이식 |
| 비밀번호 암호화 라이브러리 | ✅ 선택함 | `bcryptjs` (cost 12) — 이유는 설명서 ⓑ 참고 |
| 인증 방식 | ✅ 선택함 | 세션(서버 보관 랜덤 토큰, httpOnly 쿠키) — JWT 대신 선택한 이유는 설명서 ⓑ 참고 |
| 테스트 계정 2개 | ✅ 만들어서 검증함 | alice/bob으로 카드4 전체 검증 완료 (`evidence.txt`) |
| 네트워크/API 로그 캡처 | ✅ 완료 | `evidence.txt`, `AUTH_설명서.md`ⓓ에 성공/거절 나란히 기록 |

## 3. 카드별 실행 체크리스트 — 상태

### 카드 1~2: 인증 방식 선택 & 비밀번호 단방향 암호화
- [x] 인증 라이브러리 선택 + 이유 1문장 (`AUTH_설명서.md`)
- [x] bcrypt(salt 포함) 적용 — 동일 비밀번호도 계정마다 저장값 다름을 실증 (`evidence.txt`)
- [x] 로그/DB/문서 어디에도 비밀번호 평문·토큰 원문 없음 (직접 grep으로 확인함)

### 카드 3: 인증 유지 및 로그아웃 처리
- [x] 세션 방식 채택 + TTL 설정(`SESSION_TTL_MS`, 기본 30분)
- [x] 로그인 상태 성공 응답 vs 로그아웃 후 거절 응답 나란히 기록
- [x] 세션 값이 URL에 실리지 않음(httpOnly 쿠키로만 전달) — Git/프론트 코드에 비밀키 자체가 없음(불투명 토큰 방식이라 서명 비밀키가 존재하지 않음)

### 카드 4: 인가 및 타인 데이터 접근 차단
- [x] 계정 A/B 생성
- [x] A→B 읽기/수정/삭제 시도 → 전부 404로 거절 확인
- [x] 반대 방향(B→A, 존재하지 않는 ID)도 동일하게 거절 확인
- [x] 목록 조회에 타 계정 자료 미포함 확인
- [x] 거절 전후 자료 무결성(변조 안 됨) 확인

### 카드 5: 5일 실사용 데이터 & 설명서 완성
- [x] 설명서 6항목 틀 작성 (`AUTH_설명서.md`)
- [ ] **5일 실사용 기록** — 코드로 대신할 수 없는 부분입니다. 배포 후 실제 서로 다른 5일 동안 사용해야 합니다.
- [ ] 2일차~3일차 사이 집계 규칙 1개 변경 + 이유 기록
- [ ] 화면 합계/평균과 수기 검산 대조
- [ ] JSON 내보내기 파일 1개 준비 (기능은 구현됨, 실행만 하면 됨 — `/api/export` → "JSON 파일 내보내기" 버튼)

---

## 4. 남은 일 (사람이 직접 해야 하는 것)

1. **과제6 Git 저장소에 이 파일들을 이어 붙이기** — 새 저장소로 만들면 T07-C77/C78(과제6 커밋이 조상으로 포함)을 못 채웁니다. `README.md`의 "시작 전 꼭 확인" 참고.
2. **배포** — Render/Railway 등에 올려서 "새 시크릿 창에서 계정 없이 열리는" 공개 주소 만들기 (`README.md`의 "배포" 참고).
3. **회원가입 화면에서 실제로 계정 만들고 5일간 사용** — 서로 다른 실제 날짜 5일, 3일차 진입 전 규칙 변경 1회.
4. **`AUTH_설명서.md`의 ⓔ(AI와 나) 항목을 본인이 실제로 판단했던 과정으로 다시 쓰기** — 지금은 초안/예시입니다.
5. **비밀번호 정책 등 추가로 더 막고 싶은 게 있다면** — ⓕ에 정직하게 "아직 못 막은 것"으로 남겨두고, 여유가 있으면 무차별 대입 방지(예: `express-rate-limit`) 정도는 추가로 붙여볼 만합니다(필수는 아님).

---

## 5. 파일 위치

```
plando-diary/
├── server.js, store.js, package.json, .env.example, .gitignore
├── data/db.json              (실행 시 자동 생성, 커밋 금지)
├── public/login.html, index.html
├── AUTH_설명서.md              ← 제출용 핵심 문서
├── evidence.txt                ← 카드2~4 실제 curl 증거 원본(마스킹됨)
├── README.md                    ← 실행/배포/5일가이드
└── 체크리스트_진행상황.md        ← 이 문서
```
