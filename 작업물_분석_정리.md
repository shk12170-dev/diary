# T07(과제7) 작업물 분석 정리

작성일: 2026-09-09 | 작성 목적: 폴더 안에 흩어져 있는 작업물을 확인하고 현재 상태를 정리

---

## 1. 한눈에 보는 결론

이 폴더 안에는 **과제7("로그인/인증 붙이기")을 시도한 서로 다른 3벌의 구현체**가 동시에 존재합니다.
과제6(`index.html`, Plan-Do-See 다이어리, localStorage 전용)을 기반으로,

| # | 구현체 | 위치 | 인증 방식 | 상태 |
|---|---|---|---|---|
| A | **plando-diary** (Express 단일 서버) | `plando-diary/` | 세션 쿠키(httpOnly, bcryptjs) | ✅ 완결됨·검증 증거(evidence.txt) 있음 |
| B | **Next.js + Supabase 클라이언트** | `app/`, `lib/` | (인증 없음, DB 직접 조회) | ⚠️ 미완성·연결 안 됨 |
| C | **Vercel 서버리스 함수 + JWT** | `api/`, `contracts/`, 루트의 `과제7_인증구현설명서.md` | Bearer JWT + bcrypt + Postgres | ⚠️ 마이그레이션 도중 중단 |

세 구현체의 인증 방식(세션 vs JWT)이 서로 다르고, 설명 문서도 서로 다른 버전을 설명하고 있어
**제출 전에 반드시 하나를 선택하고 나머지를 정리해야** 채점자가 헷갈리지 않습니다.

---

## 2. 구현체별 상세

### A. `plando-diary/` — Express 세션 인증 버전 (가장 완성도 높음)

```
plando-diary/
├── server.js, store.js         # Express 백엔드, JSON 파일 저장소
├── public/login.html, index.html
├── AUTH_설명서.md               # ⓐ~ⓕ 항목 초안 완료
├── evidence.txt                 # alice/bob 계정으로 실제 curl 검증 로그
├── 체크리스트_진행상황.md
└── README.md                    # 로컬 실행/배포(Render/Railway) 안내
```

- **인증**: 서버 메모리 세션 + httpOnly 쿠키, TTL 기본 30분(`SESSION_TTL_MS`)
- **비밀번호**: `bcryptjs` (cost 12)
- **인가 검증 완료 항목** (`evidence.txt` 기준):
  - 동일 비밀번호 → 계정마다 해시값 다름 (Salt 확인)
  - 로그인 상태 200 vs 로그아웃 후 같은 쿠키로 401
  - 타인 자료 조회/수정/삭제 시도 → 모두 404
  - 반대 방향(B→A), 존재하지 않는 ID도 동일하게 404
  - 목록 조회에 타 계정 자료 미포함
  - 세션 만료(TTL) 테스트: 3초 설정 후 직후 200, 4초 뒤 401
  - 세션 값이 URL/응답 바디에 없고 `Set-Cookie`로만 전달됨을 확인
- **README.md에 명시된 주의사항**: Vercel은 서버리스라 인메모리 세션 Map이 요청마다 초기화될 수 있어
  이 구조에는 **부적합**하다고 스스로 밝히고 있음 → Render/Railway 같은 상시 구동 서버 권장.
- **남은 일**: 5일 실사용 기록은 아직 서로 다른 실제 날짜로 완료되지 않음(코드로 대신할 수 없는 부분).

### B. `app/` + `lib/` — Next.js + Supabase (미완성)

```
app/layout.js, app/page.js   # 'use client', supabase.from('todos').select('*')
lib/supabase (1).js          # 실제 파일명에 " (1)"이 붙어 있음
```

- **문제**: `app/page.js`가 `import { supabase } from '../lib/supabase'` 를 하는데
  실제 파일명은 `lib/supabase (1).js` 입니다. **경로가 일치하지 않아 빌드/런타임에서 모듈을 찾지 못합니다.**
  (다운로드 시 동명 파일이 있어 자동으로 `(1)`이 붙은 것으로 보임 — 이름을 `supabase.js`로 바꿔야 함)
- **인증 로직 자체가 없음** — 그냥 Supabase 클라이언트로 `todos` 테이블을 읽어오기만 하는 초기 프로토타입 수준.
- `.env.local`이 루트에 있으나(git에서 추적 제외됨, 안전) Supabase 키가 실제로 연결되어 있는지는 이 코드만으로 확인 불가.

### C. `api/`, `contracts/` — Vercel 서버리스 + JWT (마이그레이션 도중 정지 상태)

```
api/
├── README_(깨짐).md        # 배포 전 체크리스트 — "이 파일들을 루트로 옮기라"고 직접 안내함
├── index.html               # 로그인/회원가입 UI가 포함된 완성형 프론트엔드 (최신, 09-09 15:16)
├── auth/{login,logout,register}.js
├── lib/requireAuth.js       # JWT 검증 미들웨어
├── todos/index.js, todos/[id].js, todos/[id]/{complete,revert}.js
├── pds-schema-v2.json       # contracts/pds-schema-v2.json 과 내용 동일
└── api/                     # ⚠️ auth/·todos/ 가 통째로 중복 존재 (api/api/...)
```

- **인증**: `jsonwebtoken`(24시간 만료) + `bcrypt`(salt rounds 10), 헤더 `Authorization: Bearer <token>`
- **인가**: `lib/requireAuth.js`가 토큰 검증 후 `userId`를 꺼내 모든 쿼리에 `WHERE user_id = ...` 강제 적용,
  타인 자료는 403이 아닌 **404**로 응답(정보 노출 방지 — 근거가 `과제7_인증구현설명서.md`에 상세히 기술되어 있음)
- **git 상태와의 관계**: 기존 단일 파일 `api/auth.js`, `api/todos.js`, 루트 `login.html`이 **삭제된 상태**인데,
  이는 실수가 아니라 `api/README_...md`의 1번 지침("예전 `api/auth.js`는 삭제하고 `api/auth/register.js` 등으로 교체하라")을
  그대로 따른 것입니다. 즉 **삭제 자체는 의도된 마이그레이션 작업**입니다.
- **미완료된 부분 (README가 요구하지만 아직 안 된 것)**:
  1. `api/README_...md`는 최종 구조를 저장소 **루트**에 `/index.html`, `/api/auth/...`, `/lib/requireAuth.js` 로 두라고
     명시하는데, 지금은 전부 `api/` 폴더 **한 단계 안쪽**(`api/index.html`, `api/auth/...`, `api/lib/requireAuth.js`)에 있음.
  2. `api/auth/`와 `api/api/auth/`(그리고 `todos/`)가 **완전히 동일한 내용으로 중복** 저장되어 있음 — 정리 필요.
  3. `JWT_SECRET` 환경변수가 없으면 코드에 하드코딩된 `'secret-key'`로 동작 — README 자신이 위험하다고 경고.
  4. Postgres 테이블(`users`, `plans`, `todos` 등)이 실제로 생성되어 있는지는 코드만으론 확인 불가 (README에 CREATE TABLE 문 제공됨).

---

## 3. 문서 간 불일치

| 문서 | 설명 대상 | 인증 방식 서술 |
|---|---|---|
| `과제7_인증구현설명서.md` (루트) | 구현체 C (JWT) | JWT + bcrypt |
| `plando-diary/AUTH_설명서.md` | 구현체 A (세션) | 세션 + bcryptjs |
| `과제7_체크리스트_진행상황.md` (루트) | 구현체 A를 기준으로 작성됨 | "세션(서버 보관 랜덤 토큰, httpOnly 쿠키) — JWT 대신 선택" |
| `5일_실사용_기록_및_규칙변경.md` | 구현체 C 관련 작업 로그(JWT 디버깅 언급) | - |

→ **체크리스트 문서(A 기준)와 인증구현설명서(C 기준)가 서로 다른 구현체를 설명하고 있어 그대로 제출하면 채점자가 혼란스러워할 가능성이 높습니다.**

---

## 4. 기타 파일

- 최상위 PNG 스크린샷 14개는 git에서 `deleted`로 표시되지만, 동일한 파일명으로 `image/` 폴더에 그대로 존재합니다 → **삭제가 아니라 이동**으로 보이며 내용 손실은 없어 보입니다.
- PDF 5종(`AI 3줄 보고서.pdf`, `검증안내서.pdf`, `스크립트 모양 글자와 비밀값 점검 결과.pdf`, `웹페이지 새로고침 후 그대로인 값 결과.pdf`, `웹페이지 실행화면.pdf`, `짧은 확인 방법.pdf`)와 `할일 목록 5개 구현 결과.json`, `pds-diary-2026-09-04.json`은 제출/검증용 캡처 자료로 보이며 별도 분석은 하지 않았습니다(내용 미검토).
- 루트 `README.md`는 여전히 과제6 시절 배포 주소만 안내하고 있어, 과제7 관련 안내(로그인 필요 등)는 반영되어 있지 않습니다.

---

## 5. 과제7 체크리스트 진행 상황 (구현체 A 기준, `과제7_체크리스트_진행상황.md` 요약)

- [x] 인증 방식 선택 + 이유
- [x] bcrypt(salt) 적용 및 실증
- [x] 세션 유지·TTL·로그아웃 처리
- [x] 인가(타인 데이터 차단) A↔B 상호 테스트 — 전부 404
- [x] 설명서 6항목(ⓐ~ⓕ) 초안
- [ ] **5일 실사용 기록** — 서로 다른 실제 5일 동안 사용해야 함(대기 중)
- [ ] 2~3일차 사이 집계 규칙 변경 1회 + 이유 기록
- [ ] 손 계산 vs 화면 집계 대조
- [ ] JSON 내보내기 파일 제출 첨부
- [ ] **과제6 저장소 이어받기 확인** — 새 저장소로 만들면 T07-C77/C78(과제6 커밋이 조상으로 포함되어야 함) 조건 불충족 가능
- [ ] 배포(Render/Railway 등, "새 시크릿 창에서 계정 없이 열리는" 공개 주소)
- [ ] `AUTH_설명서.md` ⓔ(AI와 나) 항목을 본인 언어로 재작성

> 참고: `5일_실사용_기록_및_규칙변경.md`에 Day1~Day5 기록과 규칙 변경, 검산 결과가 이미 표로 작성되어 있습니다.
> 다만 이 문서는 구현체 C(JWT) 작업 로그를 담고 있어, 최종 제출 구현체와 문서를 맞출 필요가 있습니다.

---

## 6. 권장 다음 단계

1. **구현체 하나를 최종안으로 선택**하세요.
   - 지금 당장 채점 가능한 완결 상태는 **A(`plando-diary`, 세션 방식)**입니다 — 코드·설명서·증거(evidence.txt)가 다 맞아떨어집니다.
   - **C(JWT + Vercel)**로 갈 경우, 아래 정리가 끝나야 A와 동등한 완성도가 됩니다:
     - `api/` 안의 파일들을 저장소 루트로 이동 (`api/index.html`→`/index.html`, `api/auth/*`→`/api/auth/*`, `api/lib/*`→`/lib/*`)
     - 중복된 `api/api/` 폴더 삭제
     - `JWT_SECRET`을 배포 환경변수로 실제 설정
     - Postgres 테이블 생성 여부 확인
   - **B(Next.js+Supabase)**는 인증 로직이 아예 없고 import 경로도 깨져 있어, 별도 계획 없이는 과제7 제출용으로 추천하지 않습니다.
2. 선택하지 않는 구현체는 폴더째 제거하거나 `_archive/`로 옮겨 채점자가 혼동하지 않게 정리하세요.
3. 최종 선택본 기준으로 `과제7_인증구현설명서.md`와 `과제7_체크리스트_진행상황.md`를 하나로 통일하세요(현재 서로 다른 구현체를 설명 중).
4. 남아 있는 "사람이 직접 해야 하는 일"(5일 실사용, 배포, ⓔ 항목 본인 언어로 재작성)은 코드로 대신할 수 없으므로 별도로 진행하세요.
